<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/progressBtSplashscreenCl"
    android:layout_width="match_parent"
    android:minHeight="50dp"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/progress_button_background"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:id="@+id/labelRl"
        android:layout_width="50dp"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/progress_button_icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_height="0dp">

        <ImageView
            android:id="@+id/labelIc"
            android:layout_width="@dimen/iconSize"
            android:layout_height="@dimen/iconSize"
            tools:src="@drawable/ic_check_circle_outline_24px"
            android:layout_centerInParent="true"/>

    </RelativeLayout>

    <TextView
        android:id="@+id/labelTv"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/labelRl"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text = "Profilul tau"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:paddingLeft="50dp"
        android:paddingRight="50dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>